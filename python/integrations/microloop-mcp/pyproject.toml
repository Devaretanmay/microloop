[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "microloop-mcp"
version = "0.1.1"
description = "Microloop integration for MCP"
authors = [{ name = "Microloop Team" }]
license = "Apache-2.0"
requires-python = ">=3.9"
dependencies = [
    "microloop>=0.1.1"
]
