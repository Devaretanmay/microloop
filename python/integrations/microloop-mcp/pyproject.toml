[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "microloop-mcp"
version = "0.1.0"
description = "Microloop integration for MCP"
authors = [{ name = "Microloop Team" }]
requires-python = ">=3.9"
dependencies = [
    "microloop-core>=0.1.1"
]
