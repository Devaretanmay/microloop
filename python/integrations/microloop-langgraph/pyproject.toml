[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "microloop-langgraph"
version = "0.1.1"
description = "Microloop integration for LangGraph"
authors = [{ name = "Microloop Team" }]
requires-python = ">=3.9"
dependencies = [
    "langgraph>=0.0.1",
    "microloop>=0.1.1"
]
