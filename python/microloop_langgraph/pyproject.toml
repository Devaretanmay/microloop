[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "microloop-langgraph"
version = "0.1.1"
description = "LangGraph adapter for Microloop"
readme = "README.md"
authors = [{ name = "Tanmay Devare" }]
requires-python = ">=3.8"
dependencies = [
    "microloop-core>=0.1.1",
    "langgraph>=0.0.1"
]
