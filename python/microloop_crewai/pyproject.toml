[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "microloop-crewai"
version = "0.1.1"
description = "CrewAI adapter for Microloop"
readme = "README.md"
authors = [{ name = "Tanmay Devare" }]
requires-python = ">=3.8"
dependencies = [
    "microloop-core>=0.1.1",
    "crewai>=0.1.0"
]
