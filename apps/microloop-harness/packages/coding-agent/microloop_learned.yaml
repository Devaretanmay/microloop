# Auto-generated by microloop learn
# Based on analysis of 12 tool calls across 2 tools
#
# Tune these values or delete this file to regenerate.
# Docs: https://github.com/Devaretanmay/microloop
---
defaults:
  trajectory_gate:
    max_repeats: 7
    count_mode: errors_only

tools:
  - name: bash
    trajectory_gate:
      max_repeats: 9
      volatile_fields: ["command"]
      error_detection:
        json_path: "$.error"
    # calls=7, repeats=0, errors=1, error_rate=14.3%
  - name: read
    trajectory_gate:
      max_repeats: 7
      volatile_fields: ["path"]
    # calls=5, repeats=0, errors=0, error_rate=0.0%

# Summary: 2 tools analyzed | 12 total calls | 0 loops detected | 8.3% error rate
